[package]
name = "voxel"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
bevy = { version = "0.17.2", features = ["bevy_render"] }

[dev-dependencies]
bevy_brp_extras = "0.17.0"
engine = { path = "../engine" }
noise = "0.9"